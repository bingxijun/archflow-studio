interface:
  display_name: "ArchFlow Studio"
  short_description: "Automate CAD, SketchUp, regulations, and concept renders"
  default_prompt: "Use $archflow-studio to inspect my CAD/SketchUp workflow, build a traceable model, and generate a concept render from a selected view."

policy:
  allow_implicit_invocation: true
